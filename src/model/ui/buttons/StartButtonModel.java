package model.ui.buttons;

/**
 * Bottone per iniziare la partita dal menu'
 */
public class StartButtonModel extends CustomButtonModel{
    public StartButtonModel(int x, int y, int width, int height) {
        super(x, y, width, height);
    }
}
